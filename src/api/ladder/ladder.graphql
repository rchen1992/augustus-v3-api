type Ladder implements Node {
    id: ID!
    ladderName: String!
    inviteToken: String!
    createdAt: String
    updatedAt: String
    userRating: Int
    userRatingDelta: Int
}

extend type Query {
    ladders: [Ladder!]!
    ladder(id: ID!): Ladder
}
