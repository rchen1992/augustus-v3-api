type Ladder implements Node {
    id: ID!
    ladderName: String!
    inviteToken: String!
    createdAt: String
    updatedAt: String
}

extend type Query {
    ladders: [Ladder!]!
    ladder(id: ID!): Ladder
}
