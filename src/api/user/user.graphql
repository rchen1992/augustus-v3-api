type User implements Node {
    id: ID!
    email: String
    userName: String!
    avatarUrl: String
    createdAt: String
    updatedAt: String
}

extend type Query {
    users: [User!]!
    user(id: ID!): User
}
