type User implements Node {
    id: ID!
    email: String
    userName: String!
    avatarUrl: String
    ladders: [Ladder!]!
    matches(offset: Int, limit: Int): [Match!]!
    createdAt: String
    updatedAt: String
    rating: Int
    ratingDelta: Int
}

extend type Query {
    users: [User!]!
    user(id: ID!): User
    me: User
}
