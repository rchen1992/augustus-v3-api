type Match implements Node {
    id: ID!
    tied: Boolean!
    createdAt: String
    updatedAt: String
    user1: User!
    user2: User!
    ladder: Ladder!
    winner: User!
    loser: User!
}

extend type Query {
    matches: [Match!]!
    match(id: ID!): Match
}
