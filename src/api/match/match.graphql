type Match implements Node {
    id: ID!
    tied: Boolean!
    createdAt: String
    updatedAt: String
    ladder: Ladder!
    winner: User!
    loser: User!
}

extend type Query {
    matches: [Match!]!
    match(id: ID!): Match
}
